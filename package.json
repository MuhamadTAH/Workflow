{
  "name": "workflow-platform",
  "version": "1.0.0",
  "description": "Full-stack workflow builder platform",
  "scripts": {
    "build": "echo 'Backend deployment - no build required'",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install": "cd backend && npm install"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MuhamadTAH/Workflow.git"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}