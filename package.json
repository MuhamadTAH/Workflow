{
  "name": "workflow-platform",
  "version": "1.0.0",
  "description": "Full-stack workflow builder platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview",
    "dev": "cd frontend && npm run dev"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MuhamadTAH/Workflow.git"
  }
}