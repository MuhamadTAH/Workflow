[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm']

[phases.build]
cmds = ['echo "No build required for backend"']

[phases.install]
cmds = ['cd backend && npm ci --only=production --no-audit']

[start]
cmd = 'cd backend && npm start'

[variables]
NODE_ENV = 'production'